#### ssh connection:
```
ssh <user>@4.tcp.eu.ngrok.io -p 10264
```

#### starting ssh
```
sudo systemctl enable --now sshd
sudo systemctl start sshd
```

#### starting ngrok
```
ngrok tcp 2200
```

#### users passwd
